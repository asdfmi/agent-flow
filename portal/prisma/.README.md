## Module Overview
- Prisma schema and local database assets defining portal persistence.

## Responsibilities
- Describe database models for workflows, steps, runs, and metrics.
- Store the development SQLite database (`dev.sqlite`) for local use.

## Interfaces
- `schema.prisma` – Prisma data model consumed by `pnpm --dir portal exec prisma db push`.
- Environment variable `DATABASE_URL` controls the actual database target.

## Depends On
- Prisma CLI/client tooling and the surrounding server code that issues queries.

## Used By
- Portal server via `server/lib/prisma.js`.
- Developers running migrations or inspecting the schema.
